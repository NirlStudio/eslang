(global x 1)
(++ x)
(print value x)

(let increment (= ()
  (++ x).
($increment)
(print value x)

(let increment (= ()
  (let x 10)
  (++ x),
  (print value (global x),
  (print value x).

($increment)
($increment)
(print value x)
