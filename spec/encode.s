(define "string" (= ()
).

(define "number" (= ()
).

(define "date" (= ()
).

(define "range" (= ()
).

(define "symbol" (= ()
).

(define "tuple" (= ()
).

(define "operator" (= ()
).

(define "lambda" (= ()
).

(define "function" (= ()
).

(define "object" (= ()
).

(define "module" (= ()
).

(define "array" (= ()
).

(define "set" (= ()
).

(define "map" (= ()
).

(define "class" (= ()
).

(define "device" (= ()
).
