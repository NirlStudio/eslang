(define "constants" (= ()
  (should "have constant MAX_BITS" (= ()
    (assert ((Math pow 2 31) - 1) (` (Int MAX_BITS),
).

(define "functions" (= ()
  (should "have function is-safe" (= ()
    (assert (` (Int is-safe 128),
).
