(define "constants" (= ()
  (should "have constant E" (= ()
    (assert ((math E) is-a number),
).

(define "functions" (= ()
  (should "have function abs" (= ()
    (assert (:(math "abs") is-a lambda),
).
