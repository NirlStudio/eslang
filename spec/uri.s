(define "functions" (= ()
  (should "include function 'encode'" (= ()
    (assert (` (:(uri "encode") is-a Function),
  ),
  (should "include function 'decode'" (= ()
    (assert (` (:(uri "decode") is-a Function),
  ),
  (should "include function 'escape'" (= ()
    (assert (` (:(uri "escape") is-a Function),
  ),
  (should "include function 'unescape'" (= ()
    (assert (` (:(uri "unescape") is-a Function),
  ),
).
