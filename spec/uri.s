(define "functions" (= ()
  (should "include function 'encode'" (= ()
    (assert (:(uri "encode") is-a lambda),
  ),
  (should "include function 'decode'" (= ()
    (assert (:(uri "decode") is-a lambda),
  ),
  (should "include function 'escape'" (= ()
    (assert (:(uri "escape") is-a lambda),
  ),
  (should "include function 'unescape'" (= ()
    (assert (:(uri "unescape") is-a lambda),
  ),
).
